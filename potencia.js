const w=prompt('digite um numero')
const x=prompt('digite outro')

function caucularproduto (x,y){
    return x + y + y
}
const y= caucularproduto(x, y) 
alert(y)

function caucular(x,y){
    Math.pow(x, y)
}
const z=caucular(x,y)
